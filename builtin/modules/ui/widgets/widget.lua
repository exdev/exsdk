-- ======================================================================================
-- File         : widget.lua
-- Author       : Wu Jie 
-- Last Change  : 11/19/2013 | 22:21:58 PM | Tuesday,November
-- Description  : 
-- ======================================================================================

local __M = {}

-- ------------------------------------------------------------------ 
-- Desc: 
-- ------------------------------------------------------------------ 

local widget = class ({
    __typename = "widget",

    -- constructor & destructor
    __init = function ( _self, _x, _y, _w, _h )
        _self.x = _x
        _self.y = _y
        _self.width = _w
        _self.height = _h
    end,

    --/////////////////////////////////////////////////////////////////////////////
    -- properties
    --/////////////////////////////////////////////////////////////////////////////

    name = "none",

    x = 0,
    y = 0,
    width = 0,
    height = 0,

    enabled = true, -- if recieve events
    actived = true,

    parent = nil,
    children = {},

    --/////////////////////////////////////////////////////////////////////////////
    -- functions
    --/////////////////////////////////////////////////////////////////////////////


    --/////////////////////////////////////////////////////////////////////////////
    -- events
    --/////////////////////////////////////////////////////////////////////////////

    -- on_focus ( _self )
    -- on_unfocus ( _self )
    -- on_active ( _self )
    -- on_deactive ( _self )
    -- on_hover_in ( _self )
    -- on_hover_out ( _self )
    -- on_hover_move ( _self )
    -- on_press_down ( _self )
    -- on_press_up ( _self )
    -- on_mouse_wheel ( _self )
    -- on_key_down ( _self )
    -- on_key_up ( _self )
}) 
__M.widget = widget

--/////////////////////////////////////////////////////////////////////////////
-- 
--/////////////////////////////////////////////////////////////////////////////

return __M
